apiVersion: apps/v1
kind: Deployment
metadata:
  name: friendbook
spec:
  selector:
    matchLabels:
      app: friendbook
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: friendbook
    spec:
      containers:
      - name: friendbook
        image: joeygeofrey/friendbook
        imagePullPolicy: Always
        ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: friendbook-lb-svc
  labels:
    app: friendbook
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: friendbook
  ports:
  - name: HTTP
    port: 80
    targetPort: 80
